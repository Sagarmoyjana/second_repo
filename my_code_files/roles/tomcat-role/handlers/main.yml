---
# handlers file for tomcat-role

- name: reload systemd
  systemd:
    daemon_reload: yes

- name: restart tomcat
  systemd:
    name: tomcat
    state: restarted
    enabled: yes

- name: start tomcat
  systemd:
    name: tomcat
    state: started
    enabled: yes
